# プロジェクト仕様書 & 開発ガイドライン (SPEC.md)

本ドキュメントは「社内ナレッジSNS（寺子屋SNS）」の全体仕様、アーキテクチャ、および開発・運用における共通ルールをまとめたものです。

---

## 1. プロジェクト概要

- **名称**: 社内ナレッジSNS / 寺子屋SNS
- **目的**: 社内でのタイムライン型ナレッジ共有、社内掲示板、スケジュール・施設予約、業務日報・週報、チャット、ワークフロー決裁、ファイル管理を一元化するポータルアプリ。
- **対応環境**:
  - Web ブラウザ (デスクトップ・タブレット・スマートフォン)
  - PWA (Progressive Web Apps) インストール対応
  - 社内 Synology NAS / オンプレミス環境 / クラウドコンテナ環境

---

## 2. 開発ルール・合意事項

### 1) `server.ts` と `RecommendServerCode.ts` の完全同期 & 更新日時の明記
- `server.ts` を修正または拡張した場合は、**必ず `/src/components/RecommendServerCode.ts` にも最新のコードを反映**します。
- `RecommendServerCode.ts` を更新する際は、**ファイル冒頭のコメントヘッダーに更新日時（例: `YYYY年M月D日`）と主な対応内容を明記**します。
- これにより、社内サーバー管理者やユーザーが管理画面（設定や推奨コード表示UI）から最新のバックエンドコードを即座に確認・コピー・配備でき、コードの更新履歴や世代管理を容易にします。

### 2) パッケージ追加時の `package.json` 明示
- 新しい npm パッケージを追加（または更新）する必要が生じた場合、**追加したパッケージの名称・バージョン・選定理由、および更新された `package.json` の該当内容を明示して提示**します。

### 3) ポートとネットワーク
- アプリケーションサーバーは常に **ポート 3000**（ホスト `0.0.0.0`）で起動します。

### 4) UI/UX とデザイン基準
- アイコンライブラリは `lucide-react` で統一します。
- スタイリングは Tailwind CSS を使用し、直感的で読みやすい日本語レイアウトを維持します。
- アニメーションは `motion/react` を用いて、自然でストレスのない画面遷移を実現します。

---

## 3. システムアーキテクチャ & ディレクトリ構成

### 主要ディレクトリ一覧
```
/
├── server.ts                       # Express バックエンドサーバー（API / 静的ファイル / DB連携）
├── data/                           # ローカル JSON データ保存領域（フォールバック用）
├── public/                         # PWA マニフェスト、サービスワーカー、アイコン等
├── src/
│   ├── components/                 # React UI コンポーネント群
│   │   ├── RecommendServerCode.ts  # 社内オンプレ/NAS展開用の推奨サーバーコード
│   │   ├── Board.tsx               # 掲示板
│   │   ├── Timeline.tsx            # ナレッジ・タイムライン
│   │   ├── Calendar.tsx            # カレンダー・スケジュール
│   │   ├── DailyReport.tsx         # 業務日報・週報
│   │   ├── Workflow.tsx            # 電子決裁ワークフロー
│   │   ├── Chat.tsx                # チャット
│   │   └── ...
│   ├── types/                      # TypeScript 型定義
│   ├── utils/                      # 各種ユーティリティ（通知、ファイル、日付、メール等）
│   ├── App.tsx                     # メインアプリケーションコンポーネント
│   ├── main.tsx                    # エントリーポイント
│   └── index.css                   # Tailwind CSS グローバル設定
├── AGENTS.md                       # AIエージェント自動読み込み用ルール
├── SPEC.md                         # 本仕様書
└── package.json                    # パッケージ構成・ビルドスクリプト
```

---

## 4. 主要機能仕様

1. **タイムライン (ナレッジ共有)**
   - テキスト投稿、画像/ファイル添付、いいね・リアクション、URLプレビュー (OGP自動取得)
2. **社内掲示板 (Bulletins)**
   - カテゴリ別投稿、全社/拠点別スコープ、添付ファイル管理、ピン留め、閲覧ログ
3. **カレンダー & 施設・車両予約**
   - 月/週/日ビュー、繰り返し予定、Googleカレンダー同期、iCalインポート
4. **業務報告 (日報・週報・定期点検)**
   - 週次業務報告、メンテナンス報告、点検スケジューラー、Excel連携
5. **電子決裁ワークフロー**
   - 申請書作成、多段階承認フロー、承認状態の永続化、差し戻し/完了通知
6. **通知システム**
   - Web Push 通知 (VAPID 方式による端末プッシュ配信)
   - SMTP 社内メール通知連携 (Nodemailer)
   - 画面内通知センター
7. **ファイル管理**
   - ファイルアップロード、プレビュー、カテゴリ別管理
